const book = require('../model/bookModel');

const handleCreateNewBook = async (req, res) => {
    try {

    } catch (err) {
        res.status(500).send({ message: 'Error in Adding Book!' })
    }
}